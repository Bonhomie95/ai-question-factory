Generate exactly 10 multiple-choice HISTORY questions.

CATEGORY: "history"

STRICT OUTPUT RULES:
- Output ONLY a JSON array.
- No markdown, no explanations, no backticks.

JSON SCHEMA:
{
  "question": "string",
  "options": ["string", "string", "string", "string"],
  "answer": "string",
  "category": "history",
  "difficulty": "easy" | "medium" | "hard" | "expert" | "elite" | "legendary"
}

MANDATORY RULES:
- All 4 options MUST be unique.
- Answer MUST match exactly one option.
- Difficulty MUST be valid.
- No hallucinated or uncertain facts.
- Replace ambiguous facts immediately.

AUTO DIFFICULTY LOGIC:
easy       = widely known events or leaders  
medium     = moderately known major events  
hard       = deeper or regional history  
expert     = multi-era or non-obvious facts  
elite      = detailed empire/treaty/dynasty knowledge  
legendary  = extremely high-level advanced history  

CONTENT REQUIREMENTS:
- Ancient civilizations, empires, wars.
- Revolutions, colonization, economic shifts.
- Treaties, dynasties, global events, inventions.

Return ONLY a JSON array of 10 questions.

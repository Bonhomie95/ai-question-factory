Generate exactly 10 multiple-choice HISTORY questions.

Use the following schema for every question:

{
  "question": "string",
  "options": ["string", "string", "string", "string"],
  "answer": "string",
  "difficulty": "easy" | "medium" | "hard"
}

STRICT RULES:
- Output ONLY a JSON array of 10 objects.
- No explanation.
- No markdown.
- No backticks.
- No text before or after the JSON.
- The answer MUST match an option.
- Options MUST be 4 unique values.
- Difficulty must be one of: easy, medium, hard.

CONTENT REQUIREMENTS:
- Cover ancient civilizations, empires, wars, colonization, treaties, revolutions, dynasties, and major leaders.

RETURN ONLY THIS:
[
  {...}, {...}, {...}, {...}, {...},
  {...}, {...}, {...}, {...}, {...}
]
